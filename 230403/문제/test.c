#include <stdio.h>

int main(void)
{
	int a=3, b=5;
	int add=a+b;
	int mul=a*b;
	
	printf("%d, %d\n", add, mul);
	/*
	int a, b, r1, r2;

	printf("계산할 두 정수 입력: ");
	scanf("%d %d", &a, &b);
	r1=a+b;
	r2=a*b;
	printf("결과: %d, %d\n", r1, r2);
	
	int a, b, r1, r2;

	printf("입력: ");
	scanf("%d %d", &a, &b);
	r1=a/b;
	r2=a%b;
	printf("몫:%d 나머지:%d\n", r1, r2);

	int a, b, result;

	printf("넓이를 구할 사각형의 가로와 세로 길이 입력: ");
	scanf("%d %d", &a, &b);
	result=a*b;
	printf("사각형의 넓이: %d\n", result);
	
	
	int a, b, c, sum, result;

	printf("총점과 평균을 구할 세 정수 입력: ");
	scanf("%d %d %d", &a, &b, &c);
	sum=a+b+c;
	printf("총점:%d 평균:%d\n", sum, sum/3);
	
	
	int cm,m;

	printf("cm입력: ");
	scanf("%d", &cm);
	m=cm/100;
	cm%=100;
	printf("결과: %dm, %dcm\n", m, cm);

	int s, m, h;
	
	printf("초 입력: ");
	scanf("%d", &s);
	h=s/3600;
	s%=3600;
	m=s/60;
	s%=60;
	printf("%dh %dm %d\ns", h, m, s);*/
	return 0;
}
